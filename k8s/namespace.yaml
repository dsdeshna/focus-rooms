# ============================================================
# Kubernetes Namespace — Isolates all Focus Rooms resources
# ============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: focus-rooms
  labels:
    app: focus-rooms
    environment: production
